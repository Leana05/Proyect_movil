import { StyleSheet, Text, View, Button } from 'react-native';
import React from 'react';

const Card = ({ navigation }) => {
  return (
    <View>
      <Text>Estoy en el Card</Text>
    </View>
  );
};

export default Card;

const styles = StyleSheet.create({});
